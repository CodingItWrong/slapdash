{
  "name": "app",
  "private": "true",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "format": "npx prettier app/assets/tailwind app/javascript --write",
    "lint:format": "npx prettier app/assets/tailwind app/javascript --check",
    "lint:eslint": "eslint . --max-warnings=0",
    "lint": "yarn lint:eslint && yarn lint:format"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5"
  }
}
